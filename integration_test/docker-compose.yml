version: '3.8'

services:
  tester:
    image: python:3.11-slim
    volumes:
      - .:/app
    working_dir: /app
    networks:
      - aether-net
    # We use standard libs (urllib, smtplib) so no pip install needed
    command: python run_tests.py

networks:
  aether-net:
    external: true
